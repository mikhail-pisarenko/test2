# Function to simulate a connection to an API
def connect_to_api(api_key):
    if api_key == "wbvfilafi43fb":
        return "Connection successful!"
    else:
        return "Connection failed!"

# Call the function with the API key
print(connect_to_api("your_api_key_here"))

# Secret: api_key
api_key = "fhu9r3hfgbq384g39ugv38g"
print(f"API Key: {api_key}")
